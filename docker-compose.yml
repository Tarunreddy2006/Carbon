services:
  redis:
    image: redis:7
    container_name: carbon-redis

  api:
    build: .
    command: uvicorn carbon:app --host 0.0.0.0 --port 8000
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
    volumes:
      - ./detrixai.json:/app/detrixai.json:ro
      - ./detrixai.json:/detrixai.json:ro
    depends_on:
      - redis
    restart: always

  worker:
    build: .
    # ADDED: --pool=solo to prevent Fork Poisoning
    command: celery -A worker.celery_app worker --pool=solo --loglevel=info
    env_file:
      - .env
    environment:
      - KMP_DUPLICATE_LIB_OK=TRUE
      - OMP_NUM_THREADS=1
    volumes:
      - ./detrixai.json:/app/detrixai.json:ro
      - ./detrixai.json:/detrixai.json:ro
    depends_on:
      - redis
    restart: always

  beat:
    build: .
    command: celery -A worker.celery_app beat --loglevel=info
    env_file:
      - .env
    environment:
      - KMP_DUPLICATE_LIB_OK=TRUE
      - OMP_NUM_THREADS=1
    volumes:
      - ./detrixai.json:/app/detrixai.json:ro
      - ./detrixai.json:/detrixai.json:ro
    depends_on:
      - redis
    restart: always
